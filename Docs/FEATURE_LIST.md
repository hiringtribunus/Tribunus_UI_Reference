Feature List below: (each gets its own markdown spec)


1. App Shell + Google-Drive Layout
• Global layout: left sidebar + top bar + main content area
• Routing + page framework
• Styling tokens (Drive-like spacing, cards, hover states, smooth scrolling)
2. Projects: List + CRUD
• Projects index page (grid/list like Drive)
• Create project modal
• Rename / delete
• Search + sort
• Persist projects (even if just local DB / Supabase table)
3. Project Profile: Details + Zoning Map Placeholder
• Project “home” page
• Editable fields: name, address, sqft, etc.
• Address → placeholder zoning map panel (static image or fake tile embed)
• “Project setup complete” state
4. Data Sources: Project Library Placeholder
• Per-project “Data Sources” page (council reports, news, maps)
• Upload/add links (placeholder)
• List view + metadata (source type, date added)
• This is what the future agent will read from
5. Pro Forma / Scenario Planning (MVP Modeling UI)
• Project selector inside pro forma
• Assumptions form (a few core inputs)
• Simple outputs section (even if dummy calc v1)
• Sliders/inputs that update numbers live (basic modeling feel)
6. Tribunus AI Chat UI Template (No Agent)
• Chat thread UI + input box
• Project dropdown selector
• Mode toggles (Council Reports / Web / News) as UI only
Placeholder assistant responses (no backend intelligence)
